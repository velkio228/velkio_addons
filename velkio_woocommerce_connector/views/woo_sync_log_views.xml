<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_woo_sync_log_list" model="ir.ui.view">
        <field name="name">woo.sync.log.list</field>
        <field name="model">woo.sync.log</field>
        <field name="arch" type="xml">
            <list string="Sync Logs" create="false" edit="false">
                <field name="create_date"/>
                <field name="name"/>
                <field name="instance_id"/>
                <field name="operation"/>
                <field name="state" decoration-success="state == 'success'" decoration-danger="state == 'failed'"/>
            </list>
        </field>
    </record>

    <record id="view_woo_sync_log_form" model="ir.ui.view">
        <field name="name">woo.sync.log.form</field>
        <field name="model">woo.sync.log</field>
        <field name="arch" type="xml">
            <form string="Sync Log" create="false" edit="false">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="instance_id"/>
                        <field name="operation"/>
                        <field name="state"/>
                        <field name="create_date"/>
                    </group>
                    <group string="Message">
                        <field name="message" nolabel="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_woo_sync_log_search" model="ir.ui.view">
        <field name="name">woo.sync.log.search</field>
        <field name="model">woo.sync.log</field>
        <field name="arch" type="xml">
            <search string="Sync Logs">
                <field name="name"/>
                <field name="instance_id"/>
                <filter name="success" string="Success" domain="[('state', '=', 'success')]"/>
                <filter name="failed" string="Failed" domain="[('state', '=', 'failed')]"/>
                <group>
                    <filter name="group_instance" string="Instance" context="{'group_by': 'instance_id'}"/>
                    <filter name="group_operation" string="Operation" context="{'group_by': 'operation'}"/>
                    <filter name="group_state" string="State" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_woo_sync_log" model="ir.actions.act_window">
        <field name="name">Sync Logs</field>
        <field name="res_model">woo.sync.log</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_woo_sync_log_search"/>
    </record>
</odoo>
